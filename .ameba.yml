# This configuration file was generated by `ameba --gen-config`
# on 2018-09-13 11:39:23 +02:00 using Ameba version 0.8.0.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Problems found: 1
# Run `ameba --only Layout/TrailingBlankLines` for details
Layout/TrailingBlankLines:
  Description: Disallows trailing blank lines
  Enabled: true
  Excluded:
  - src/kimberlite/libvulkan.cr

# Problems found: 4
# Run `ameba --only Style/TypeNames` for details
Style/TypeNames:
  Description: Enforces type names in camelcase manner
  Enabled: true
  Excluded:
  - src/kimberlite/libvulkan.cr

# Problems found: 1
# Run `ameba --only Lint/UnusedArgument` for details
Lint/UnusedArgument:
  Description: Disallows unused arguments
  IgnoreDefs: true
  IgnoreBlocks: false
  IgnoreProcs: false
  Enabled: true
  Excluded:
  - crystal_lib/src/clang/cursor.cr
